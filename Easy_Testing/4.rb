assert_equal(true, list.empty?)

# `assert_empty` tests whether its first argument respond to the method `#empty?` with a `true` value.

assert_empty(list) # clearer and issues a better failure message.